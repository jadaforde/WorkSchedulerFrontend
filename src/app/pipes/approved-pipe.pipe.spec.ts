import { ApprovedPipePipe } from './approved-pipe.pipe';

describe('ApprovedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApprovedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
